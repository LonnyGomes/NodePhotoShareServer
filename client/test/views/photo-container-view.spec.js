define(['views/photo-container-view'], function (PhotoContainerViewView) {
  describe('PhotoContainerViewView', function () {
    it('exists', function () {
      expect(PhotoContainerViewView).to.be.ok;
    });
  });
});